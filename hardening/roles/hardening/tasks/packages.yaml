# roles/hardening/tasks/packages.yaml
---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Install hardened packages
  ansible.builtin.apt:
    name: "{{ required_packages }}"
    state: present
