# roles/hardening/tasks/ntp.yml
---
- name: Configure NTP server
  ansible.builtin.template:
    src: ntp.conf.j2
    dest: /etc/ntp.conf
    owner: root
    group: root
    mode: '0644'
  notify: Restart ntp
